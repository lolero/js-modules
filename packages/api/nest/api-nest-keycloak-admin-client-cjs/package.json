{
  "private": true,
  "name": "@js-modules/api-nest-keycloak-admin-client-cjs",
  "description": "Api Nest Keycloak Admin Client Cjs",
  "version": "0.0.0",
  "license": "MIT",
  "main": "build/index",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json && esbuild src/index.ts --platform=node --bundle --format=cjs --outfile=build/index.js"
  },
  "dependencies": {
    "@keycloak/keycloak-admin-client": "^24.0.4"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  },
  "eslintConfig": {}
}
