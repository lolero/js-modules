export enum Modules {
  home = 'home',
  locations = 'locations',
  users = 'users',
  segways = 'segways',
  reservations = 'reservations',
  settings = 'settings',
  help = 'help',
}

export enum SubModulesHome {
  segways = 'segways',
  locations = 'locations',
}

export enum SubModulesLocations {
  europe = 'europe',
  america = 'america',
}
