export enum Modules {
  segways = 'segways',
  home = 'home',
  locations = 'locations',
  reservations = 'reservations',
  users = 'users',
}

export enum SubModulesLocations {
  europe = 'europe',
  america = 'america',
}
