import React from 'react';
import Box from '@mui/material/Box';

const LocationsWorkspaceBox: React.FunctionComponent = () => {
  return <Box>here goes the locations page</Box>;
};

export const LocationsWorkspaceBoxRaw = LocationsWorkspaceBox;
export const LocationsWorkspaceBoxMemo = React.memo(LocationsWorkspaceBoxRaw);
export default LocationsWorkspaceBoxMemo;
