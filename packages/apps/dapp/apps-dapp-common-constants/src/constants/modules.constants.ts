export enum Modules {
  portfolio = 'portfolio',
  analytics = 'analytics',
}

export enum SubModulesPortfolio {
  tokens = 'tokens',
  transactions = 'transactions',
}

export enum SubModulesAnalytics {
  tokens = 'tokens',
  history = 'history',
  insights = 'insights',
}
